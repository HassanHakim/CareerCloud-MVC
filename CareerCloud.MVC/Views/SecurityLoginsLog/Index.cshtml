@model IEnumerable<CareerCloud.Pocos.SecurityLoginsLogPoco>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SecurityLogin.EmailAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceIP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LogonDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsSuccesful)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SecurityLogin.EmailAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceIP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LogonDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsSuccesful)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
